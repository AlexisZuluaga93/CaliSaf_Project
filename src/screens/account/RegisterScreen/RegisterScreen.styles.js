import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  image: {
    resizeMode: "contain",
    width: "100%",
    height: 150,
    marginTop: 20,
    color: "#00a680",
  },
  content: {
    marginHorizontal: 40,
  },
});
